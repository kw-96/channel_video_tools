# FFmpeg 可执行文件目录

请将以下文件放置于此目录，供程序调用：

- `ffmpeg.exe`
- `ffprobe.exe`
- `ffplay.exe`（可选）

可从 [FFmpeg 官网](https://ffmpeg.org/download.html) 或 [BtbN/ffmpeg-builds](https://github.com/BtbN/FFmpeg-Builds/releases) 下载 Windows 版本，解压后将上述 exe 放入本目录。

若系统 PATH 中已安装 FFmpeg，程序会回退使用系统版本，本目录可留空。
